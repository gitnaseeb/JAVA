/*points to remember for abstract class
 **1.child can replace parent but parent cannot replace child
 *2.parent classes should be definitely declared as abstract
 *3.what you have declared in parent that can child can access them
 *4. reusability
 *5.final method cannot be override
 *6.all business method in the parent class should be declared as final
 *overriding for only methods not variable
 *
 *MULTITHREADING
 *a sub task is called thread
 *a task in a task is called threading
 *
 *  */
